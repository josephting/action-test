name: Test
on:
  workflow_dispatch:
    inputs:
      release_github:
        type: boolean
        description: Release on GitHub
        default: true
jobs:
  publish:
    environment: Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Do something
        run: |
          echo "Hello"
